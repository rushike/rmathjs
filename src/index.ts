import {
  bignum,
  fraction,
  real, decimal, bigdecimal,
  complex
} from "./dtype";

import {
  gcd,
  lcm,
  factorial,
  powz,
  pow,
  exp,
} from "./functions/elementary"

export default {
  /** Data Types */
  bignum,
  fraction,
  real, decimal, bigdecimal,
  complex,
  /** Functions  */
  gcd,
  lcm,
  factorial,
  powz,
  pow,
  exp
}